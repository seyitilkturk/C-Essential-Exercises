#include <stdio.h>

int main()
{

float piNumber = 3.14;
float rNumber = 6;
float perimeter;
float area;

perimeter = 2 * piNumber * rNumber;
area = piNumber * rNumber * rNumber;

printf("Perimeter of the Circle = %f \n", perimeter);
printf("Area of the Circle = %f \n", area);

  return 0;
}
